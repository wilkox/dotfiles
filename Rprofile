library(stats)
library(colorout)
setOutputColors(error = c(8, 1), verbose = FALSE) # For readable error messages
Sys.setenv(TZ = "Australia/Sydney")
options(
  repos = c(CRAN = "https://cran.rstudio.com"),
  width = 76
)
